--- a/arch/mips/Kconfig
+++ b/arch/mips/Kconfig
@@ -276,6 +276,8 @@ config BMIPS_GENERIC
 	select BCM7038_L1_IRQ
 	select BCM7120_L2_IRQ
 	select BRCMSTB_L2_IRQ
+	select HAVE_PCI
+	select PCI_DRIVERS_GENERIC
 	select IRQ_MIPS_CPU
 	select DMA_NONCOHERENT
 	select SYS_SUPPORTS_32BIT_KERNEL
